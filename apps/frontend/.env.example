# Backend API URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# JWT token lifetimes — must match ACCESS_TOKEN_EXPIRE_MINUTES and
# REFRESH_TOKEN_EXPIRE_DAYS in the backend .env
NEXT_PUBLIC_ACCESS_TOKEN_EXPIRE_MINUTES=15
NEXT_PUBLIC_REFRESH_TOKEN_EXPIRE_DAYS=7
